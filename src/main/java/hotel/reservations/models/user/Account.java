/**
 * @file Account.java
 * @author Joshua Wellman
 * @brief Provides the application with an enum type to represent that state of the application's logged-in state.
 */

package hotel.reservations.models.user;

public enum Account {
    GUEST,
    CLERK,
    MANAGER,
    ADMIN
}
